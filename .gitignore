# Local build config (may contain API keys)
local.properties

# Build outputs
build/
app/build/
.gradle/
clone.py
# Android SDK / JDK (bundled toolchain, not part of source)
.sdk/
.agent
# Claude Code
.claude/

# Local scratch / test data (not committed)
local/
scripts/thor-wifi-connect.sh

# Python cache from scripts/
__pycache__/
*.pyc

# Release APK (distributed via GitHub Releases, not committed)
*.apk

# Icon source images
img/

# Android Studio
.idea/
*.iml
.DS_Store

# Debug screenshots and session logs
dirty_*.png
ocr_*.png
session_*.jsonl

# OS
Thumbs.db
